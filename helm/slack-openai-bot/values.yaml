replicaCount: 1

image:
  repository: ghcr.io/dlunch/bot
  pullPolicy: Always
  tag: "latest"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

podAnnotations: {}
podLabels: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  fsGroup: 1000

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: false
  capabilities:
    drop:
      - ALL

resources: {}

nodeSelector: {}
tolerations: []
affinity: {}

env:
  maxThreadHistory: 20
  slackStreamUpdateMs: 800
  discordStreamUpdateMs: 800

auth:
  # Set this to use a pre-created secret instead of chart-managed one.
  # The secret keys must include at least one of:
  # - CODEX_ACCESS_TOKEN
  # - CODEX_REFRESH_TOKEN
  # Optional keys: CODEX_ACCOUNT_ID, CODEX_REFRESH_CLIENT_ID.
  existingSecret: ""
  accessToken: ""
  refreshToken: ""
  accountId: ""
  refreshClientId: ""

config:
  # Set this to use a pre-created secret containing services.json.
  servicesExistingSecret: ""

  services:
    slack: []
    discord: []
    irc: []
