apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "slack-openai-bot.authPersistenceClaimName" . }}
  labels:
    {{- include "slack-openai-bot.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.auth.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.auth.persistence.size }}
