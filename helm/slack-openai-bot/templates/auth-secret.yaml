{{- if not .Values.auth.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "slack-openai-bot.authSecretName" . }}
  labels:
    {{- include "slack-openai-bot.labels" . | nindent 4 }}
type: Opaque
stringData:
  CODEX_ACCESS_TOKEN: {{ required "auth.accessToken is required when auth.existingSecret is empty" .Values.auth.accessToken | quote }}
  {{- if .Values.auth.accountId }}
  CODEX_ACCOUNT_ID: {{ .Values.auth.accountId | quote }}
  {{- end }}
{{- end }}

