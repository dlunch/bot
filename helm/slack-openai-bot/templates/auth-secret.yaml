{{- if not .Values.auth.existingSecret }}
{{- if not .Values.auth.refreshToken }}
{{- fail "auth.refreshToken is required when auth.existingSecret is empty" }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "slack-openai-bot.authSecretName" . }}
  labels:
    {{- include "slack-openai-bot.labels" . | nindent 4 }}
type: Opaque
stringData:
  CODEX_REFRESH_TOKEN: {{ .Values.auth.refreshToken | quote }}
{{- end }}
