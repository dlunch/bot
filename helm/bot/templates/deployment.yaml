apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bot.fullname" . }}
  labels:
    {{- include "bot.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "bot.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "bot.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: bot
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          env:
            - name: CODEX_REFRESH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "bot.authSecretName" . }}
                  key: CODEX_REFRESH_TOKEN
                  optional: true
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "bot.authSecretName" . }}
                  key: ANTHROPIC_API_KEY
                  optional: true
            - name: CODEX_REFRESH_TOKEN_FILE
              value: {{ .Values.auth.codex.refreshTokenFile | quote }}
            - name: MAX_THREAD_HISTORY
              value: {{ .Values.env.maxThreadHistory | quote }}
            - name: SLACK_STREAM_UPDATE_MS
              value: {{ .Values.env.slackStreamUpdateMs | quote }}
            - name: DISCORD_STREAM_UPDATE_MS
              value: {{ .Values.env.discordStreamUpdateMs | quote }}
          volumeMounts:
            - name: services-config
              mountPath: /app/config/services.json
              subPath: services.json
              readOnly: true
            - name: auth-state
              mountPath: {{ .Values.auth.persistence.mountPath | quote }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: services-config
          secret:
            secretName: {{ include "bot.servicesSecretName" . }}
        - name: auth-state
          persistentVolumeClaim:
            claimName: {{ include "bot.authPersistenceClaimName" . }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
